apiVersion: networking.networking.metal.ironcore.dev/v1alpha1
kind: SwitchInterface
metadata:
  labels:
    app.kubernetes.io/name: switch-operator
    app.kubernetes.io/managed-by: kustomize
  name: switchinterface-sample
spec:
  # TODO(user): Add fields here
